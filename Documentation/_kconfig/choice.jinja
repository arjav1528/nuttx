{{ choice_desc(choice) }}
{{ "=" * choice_desc(choice)|length }}

.. _{{ choice_id(choice) }}:

{% from "helper.jinja" import help, direct_dep, defaults, kconfig_definition %}
{{ help(choice) }}

Details
-------

**Type:** {{ type_str(choice) }}

{{ direct_dep(choice) }}

{% if choice.defaults %}
**Defaults:**
{% for expr, value in choice.defaults %}
- CONFIG_{{ expr.name }} → ``{{ format_default_value(value) }}``
{% endfor %}
{% endif %}

**Options:**
{% for sym in choice.syms %}
- CONFIG_{{ sym.name }}
{% endfor %}

Raw definition
~~~~~~~~~~~~~~

{{ kconfig_definition(choice) }}
